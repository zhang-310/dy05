# DESIGN.md

护肤品/彩妆类直播电商 SaaS 平台 - 设计系统

基于 **Shopify 电商设计** + **Airbnb 摄影美学** + **Intercom 对话交互** 的融合设计。

## 🎨 色彩系统

### 主色板

| 颜色 | 色值 | 用途 | 备注 |
|------|------|------|------|
| 霓虹绿 | `#00D084` | 主色、CTA 按钮、成功状态 | Shopify 标志色，购买欲强 |
| 深灰黑 | `#0F172A` | 页面背景、深色模式背景 | 电影级视觉，高端感 |
| 深灰 | `#1E293B` | 卡片、表面、对话框背景 | 分层感，易区分 |
| 中灰 | `#475569` | 次级文本、禁用状态 | 对比度 WCAG AA |
| 浅灰 | `#E2E8F0` | 边框、分割线 | 轻盈感，不突兀 |
| 金色 | `#FFD700` | 强调、奢侈品标记 | 护肤品高端气质 |
| 浅白 | `#F1F5F9` | 主文本、亮色文字 | 高对比度，易读 |
| 红色 | `#EF4444` | 错误、警告、库存预警 | 关键信息 |
| 蓝色 | `#3B82F6` | 信息、链接、次级操作 | 信息感 |

### 深色模式配置

```typescript
// 所有页面默认使用暗色模式（参考 Shopify、Spotify）
背景色：#0F172A（深灰黑）
表面色：#1E293B（深灰）
主文本：#F1F5F9（浅白）
次文本：#475569（中灰）
```

## 🔤 排版系统

### 字体族

```typescript
fontFamily: '"Inter", "Helvetica Neue", -apple-system, BlinkMacSystemFont, sans-serif'
```

### 字重规范

| 用途 | 字重 | 示例 |
|------|------|------|
| 正文 | 400 | 商品描述、评论内容 |
| 重点 | 500 | 次级标题、标签 |
| 标题 | 600-700 | 页面标题、卡片标题 |
| 浅色 | 300 | 副标题、辅助信息（Shopify 特征） |

### 字号阶梯

| 用途 | 字号 | 行高 | 字间距 |
|------|------|------|--------|
| 大标题 | 32px | 1.2 | -0.5px |
| 标题 | 24px | 1.3 | -0.3px |
| 小标题 | 18px | 1.4 | 0 |
| 正文 | 14px | 1.6 | 0.2px |
| 小文本 | 12px | 1.5 | 0 |
| 超小文本 | 11px | 1.4 | 0 |

## 📏 间距系统

**基础单位：8px**

| 变量 | 像素 | 用途 |
|------|------|------|
| xs | 4px | 微小间距（图标间、行内） |
| sm | 8px | 小间距（元素内部） |
| md | 16px | 中间距（组件间） |
| lg | 24px | 大间距（模块间） |
| xl | 32px | 超大间距（部分间） |
| 2xl | 48px | 版心内边距 |

### 内边距规范

- **按钮**：`12px 16px`（高 44px）
- **卡片**：`16px` 内边距
- **对话框**：`24px` 内边距
- **输入框**：`12px 16px`（高 44px）
- **列表项**：`12px 16px`

## 🎯 组件规范

### 按钮

**尺寸**：
- 高度：44px（最小可交互区域）
- 圆角：8px（Shopify 风格）
- 字体：600 weight, 14px

**状态**：
| 状态 | 背景 | 文字 | 投影 |
|------|------|------|------|
| 默认 | #00D084 | #0F172A | 无 |
| Hover | #00B36A | #0F172A | 0 8px 16px rgba(0, 208, 132, 0.3) |
| Active | #00984D | #0F172A | 0 4px 8px rgba(0, 208, 132, 0.2) |
| Disabled | #64748b | #94a3b8 | 无 |

**类型**：
- 主按钮（CTA）：绿色填充
- 次按钮：金色填充
- 文字按钮：绿色文字
- 危险按钮：红色填充

### 卡片

- 圆角：12px
- 边框：1px #334155
- 背景：#1E293B
- 投影：`0 4px 12px rgba(0, 0, 0, 0.3)`
- 内边距：16px
- Hover：边框变为 #00D084，投影增强

### 对话框

- 圆角：12px
- 背景：#1E293B
- 边框：1px #334155
- 遮罩：rgba(15, 23, 42, 0.7)（深半透明）
- 内边距：24px
- 标题权重：600

### 输入框

- 圆角：6px
- 高度：44px
- 边框：1px #334155
- 背景：#0F172A
- 文字色：#F1F5F9
- 焦点边框：2px #00D084
- 内边距：12px 16px

### 表格（DataGrid）

- 行高：48px
- 边框：1px #334155
- 表头背景：#1E293B
- 表头文字权重：600
- Hover 行背景：rgba(0, 208, 132, 0.05)（轻绿）
- 排序图标：#00D084

### 标签 & 徽章

- 圆角：20px（完全圆角）
- 高度：24px
- 内边距：4px 12px
- 字体：12px weight 500
- 默认：背景 #334155，文字 #F1F5F9
- 成功：背景 rgba(0, 208, 132, 0.2)，文字 #00D084
- 警告：背景 rgba(251, 191, 36, 0.2)，文字 #fbbf24

## 📱 响应式布局

### 断点

| 设备 | 宽度 | 栅格 | 内边距 |
|------|------|------|--------|
| 移动 | 320px-640px | 4 列 | 12px |
| 平板 | 641px-1024px | 8 列 | 16px |
| 桌面 | 1025px-1440px | 12 列 | 24px |
| 超宽屏 | 1441px+ | 12 列 | 32px |

### 移动优先

```css
/* 移动 320px */
.container { padding: 12px; }

/* 平板 640px+ */
@media (min-width: 640px) {
  .container { padding: 16px; }
}

/* 桌面 1024px+ */
@media (min-width: 1024px) {
  .container { padding: 24px; }
}
```

## ✨ 交互与动画

### 动画时间

- 快速反馈（加入购物车、点赞）：200ms
- 标准动画（页面转换、对话框）：300ms
- 缓慢动画（加载、长操作）：500ms

### 缓动函数

```typescript
easeInOut: 'cubic-bezier(0.4, 0, 0.2, 1)' // 标准
easeOut: 'cubic-bezier(0, 0, 0.2, 1)'     // 出现
easeIn: 'cubic-bezier(0.4, 0, 1, 1)'      // 消失
```

### 微交互示例

**添加购物车动画**：
1. 按钮变绿色 (100ms)
2. 文字变为 "已加入" (100ms)
3. 购物车数字 +1，上升动画 (300ms)
4. Toast 提示出现 (200ms)

**购物车浮球**：
- 固定位置：右下角 (bottom: 20px, right: 20px)
- 大小：48px 圆形
- Hover 放大 1.1x (200ms)
- 点击 → 抽屉从右滑入 (300ms)

## 🌙 深色模式

所有页面默认为深色模式（与 Shopify、Spotify 对齐）。

浅色模式为可选功能（将来支持）。

### 颜色映射

- `background.default` → `#0F172A`
- `background.paper` → `#1E293B`
- `text.primary` → `#F1F5F9`
- `divider` → `#334155`

## 📸 直播间页面设计

### 布局

```
┌─────────────────────────────┐
│   直播间顶部导航            │ (AppBar)
├─────────────────────────────┤
│                             │
│   直播视频区域              │ (16:9 比例)
│   [直播间名 | 人气 | 关注]  │
│                             │
├─────────────────────────────┤
│  商品展示（横向滚动）       │
│  [产品卡片] [产品卡片] ...  │
├─────────────────────────────┤
│  推荐直播列表（卡片网格）   │
│  [卡片] [卡片] [卡片]       │
│  [卡片] [卡片] [卡片]       │
├─────────────────────────────┤
│    [☆ 收藏] [💬 客服]        │
│                             │
│              [🛒 0]         │ (购物车浮球)
└─────────────────────────────┘
```

### 商品卡片

- 尺寸：220px × 280px
- 结构：产品大图 + 价格 + 优惠标签 + "加入购物车" 按钮
- 库存预警：红色标签 "仅剩 3 件"

### 支付流程

1. 点击 "加入购物车" → 按钮变绿，数字 +1
2. 点击购物车浮球 → 右侧抽屉滑入
3. 选择商品/修改数量 → 实时更新小计
4. 点击 "前往支付" → 中心对话框弹出
5. 选择支付方式 + 优惠码 → 确认支付
6. 支付成功 → 勾选动画 + 自动关闭

## 📊 数据展示

### KPI 卡片

- 尺寸：240px × 160px
- 内容：指标名 + 数值（超大字号） + 趋势（环比/同比）
- 趋势颜色：上升 → 绿色，下降 → 红色
- 投影：轻微，在深色背景下明显

### 图表

- 折线图：绿色线条（#00D084）
- 柱状图：绿色柱子（#00D084）
- 饼图：多色方案（绿色为主）
- 网格线：#334155（轻盈）

## ✅ 使用检查清单

- [ ] 所有按钮都是 44px 高度
- [ ] 所有卡片圆角 12px
- [ ] 所有对话框圆角 12px
- [ ] 颜色系统只使用定义的 9 种颜色
- [ ] 间距只使用 8px 的倍数
- [ ] 文字只使用定义的字号
- [ ] 动画时间 200ms/300ms/500ms
- [ ] 对比度 ≥ 4.5:1（WCAG AA）
- [ ] 移动端最小触摸区 44×44px

---

**参考资料**：
- Shopify 设计系统
- Airbnb 设计系统
- Material Design 3
- WCAG 2.1 无障碍标准
