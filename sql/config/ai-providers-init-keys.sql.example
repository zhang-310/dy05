-- ============================================================
-- AI 第三方 API Key 初始化（可选）
-- 使用方式：复制为 ai-providers-init-keys.sql，填入真实 Key 后执行
-- 注意：勿将含真实 Key 的文件提交到版本控制
-- ============================================================

-- 智谱 GLM / OpenClaw API Key
-- UPDATE sys_config SET config_value = 'YOUR_GLM_API_KEY' WHERE config_key = 'ai.glm.api_key' AND deleted = 0;

-- Minimax API Key
-- UPDATE sys_config SET config_value = 'YOUR_MINIMAX_API_KEY' WHERE config_key = 'ai.minimax.api_key' AND deleted = 0;

-- DeepSeek API Key（可选）
-- UPDATE sys_config SET config_value = 'YOUR_DEEPSEEK_API_KEY' WHERE config_key = 'ai.deepseek.api_key' AND deleted = 0;
