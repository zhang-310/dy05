-- ============================================================
-- 清空排品表导入数据，便于重新上传
-- 仅软删除 SKU 以 BT 开头的商品（排品表格式）
-- ============================================================

UPDATE dy_product SET deleted = 1 WHERE sku LIKE 'BT%' AND deleted = 0;
