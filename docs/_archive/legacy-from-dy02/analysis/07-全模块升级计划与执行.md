# 07-全模块升级计划与执行

> 依据 docs/analysis 下 00～06 模块深度分析，汇总 P0/P1 升级项并执行。
> 生成日期：2026-03-05

## 一、总览

| 模块 | 分析文档 | P0 项 | P1 项 | SQL 迁移 | 状态 |
|------|----------|-------|-------|----------|------|
| auth | 01-auth | 多租户隔离、Redis Token、权限缓存 | 索引、菜单缓存 | 索引 | 见迁移脚本 |
| ai | 02-ai | GET→POST、分布式锁备用、前端 API | 向量维度可配、VO | 索引 | 见迁移脚本 |
| live | 03-live | @SQLRestriction、AI 重试、N+1 | 速率限制、事务 | deleted 列 | ✅ 已执行 |
| shortvideo | 04-shortvideo | ContentAudit 占位、上传限制、Dashboard | 索引、version、事务 | 索引 | ✅ 部分已存在 |
| product | 05 | 事务、错误码 | 缓存、批量 API | — | ✅ 已执行 |
| douyin | 05 | — | 统计缓存、账号验证 | — | — |
| script | 05 | CSV 权限 | 检测缓存、历史查询 | — | ✅ 已执行 |
| copy | 05 | 审批角色验证 | 字数分词、模板应用 | — | ✅ 已执行 |
| config/storage/log | 06 | — | 版本管理、断点续传、request_body | log 字段 | 见迁移脚本 |

## 二、本轮已执行项（代码 + SQL）

### 2.1 Live 模块
- **SQL**：`sql/migrations/upgrade-analysis-2026.sql` 为 `live_monitor`、`live_session_data` 增加 `deleted INTEGER DEFAULT 0`。
- **Entity**：`LiveMonitor`、`LiveSessionData` 增加 `deleted` 字段及 `@SQLRestriction("deleted = 0")`、`@PreUpdate`。

### 2.2 Product 模块
- **Service**：`publish`/`unpublish`/`setFeatured` 已有 `@Transactional`，无需改动。
- **错误码**：库存乐观锁冲突由 `DUPLICATE_SUBMIT` 改为 `INVENTORY_CONFLICT`（3115），并已在 ErrorCode 与错误码文档中登记。

### 2.3 Script 模块
- **Controller**：违规词 CSV 导入/导出接口增加 Admin 角色校验（仅 `roleCode == "admin"` 可调用）。

### 2.4 Copy 模块
- **Controller**：审批 `save` 在审批状态为通过(1)/拒绝(0)时，校验当前用户为 Admin，否则返回 403。

### 2.5 Shortvideo 模块
- **上传**：已在 `ShortVideoUploadController` 中做图片/视频/音频大小与类型校验，无需新增。
- **ContentAudit**：保持“未配置或未实现时返回 pass”，去掉 TODO，补充说明“配置百度云后可接入真实审核”。

### 2.6 统一 SQL 迁移（upgrade-analysis-2026.sql）
- **auth**：`idx_auth_resource_type_module`、`idx_auth_role_resource_res_id`、`idx_auth_verify_code_target`。
- **ai**：`idx_kb_document_kb_status`、`idx_call_log_user_time`、`idx_evolve_task_status`。
- **live**：`live_monitor.deleted`、`live_session_data.deleted`。
- **shortvideo**：`idx_sv_vd_view_delta`（`sv_video_data(snapshot_date DESC, view_delta DESC)`）。
- **log**：`sys_operation_log.request_body`、`response_body`（可选，按需启用）。

## 三、执行方式

### 3.1 数据库迁移
```bash
# PostgreSQL，在项目根目录执行
psql -h localhost -p 5433 -U postgres -d douyin_operations -f sql/migrations/upgrade-analysis-2026.sql
```
或由 CI/运维在发布流程中执行上述脚本。

### 3.2 后端
```bash
mvn compile
mvn test
```

### 3.3 第二轮已执行（继续升级）

- **shortvideo**：`ShortVideoQuickServiceImpl.quickGenerate` 增加 `@Transactional(rollbackFor = Exception.class)`，保证项目/脚本/分镜三级创建原子性。
- **shortvideo**：`ShortVideoDashboardServiceImpl.getStats` / `getTrend` 改为真实数据：getStats 聚合 `sv_video` 播放量、成本来自 `getCostBreakdown`；getTrend 按 `sv_video_data` 日期聚合播放/点赞/评论。
- **shortvideo**：`SvVideoRepository` 新增 `findByOwnerIdAndDeleted`；`SvVideoDataRepository` 新增 `findByVideoIdInAndSnapshotDateBetween`，供 Dashboard 使用。
- **ai**：`KnowledgeBaseController.checkImportRequirements` 由 `@GetMapping` 改为 `@PostMapping`，符合统一 POST 规范（前端已用 POST，无需改）。

### 3.4 第三轮已执行（继续升级）

- **auth**：多租户数据隔离。`AuthUserService.search/getById/save` 增加参数 `callerOrganizationId`、`callerIsAdmin`；非 admin 且机构 ID 非空时，search 仅返回同机构用户，getById 仅允许查看同机构用户，save 新建时绑定当前机构、编辑时仅允许同机构用户；Controller 从 `AuthTokenFilter.getOrganizationId(request)` 传入。
- **copy**：文案模板变量格式校验。`CopyTemplateServiceImpl.save` 保存前校验 `templateContent` 中所有 `{变量名}`，变量名须匹配 `[a-zA-Z_][a-zA-Z0-9_]*`，非法则抛出 VALIDATION_FAIL。
- **live**：AI 话术生成重试。`application.yml` 增加 `resilience4j.retry.instances.liveAiRetry`（maxAttempts=3、waitDuration=2s、对 TimeoutException/IOException 等重试）；`LiveAiServiceImpl` 的 `generateOpening`、`generateProduct`、`generateTransition`、`generateClosing` 增加 `@Retry(name = "liveAiRetry")`。

### 3.5 第四轮已执行（继续升级）

- **auth**：权限校验缓存。`AuthRoleService.getResourceIdsByRoleId` 增加 `@Cacheable(value = "auth:roleResources", key = "#roleId")`；`saveRoleResources` 增加 `@CacheEvict(value = "auth:roleResources", key = "#roleId")`；`AuthPermissionServiceImpl.hasPermission` 改为调用 `authRoleService.getResourceIdsByRoleId(roleId)`，减少重复查库。
- **log**：操作日志 request/response 体。`OperationLog` 实体增加 `requestBody`、`responseBody`（各 2000 字符）；`OperationLogService.save` 增加两参数并落库；`OperationLogVO`、Filter/Listener 调用处传 `null` 占位（后续可接入 ContentCachingWrapper 等做真实采集）。
- **shortvideo**：`SvProject` 乐观锁。迁移脚本增加 `sv_project.version INTEGER NOT NULL DEFAULT 1`；Entity 增加 `@Version` 与 `version` 字段。
- **dashboard**：统一 POST。`module/dashboard` 的 `/admin/stats`、`/org/stats` 由 `@GetMapping` 改为 `@PostMapping`；`module/auth` 的 Dashboard `/admin`、`/org`、`/talent` 改为 `@PostMapping`；前端 `api/dashboard.ts` 的 getAdminDashboard/getOrgDashboard/getTalentDashboard 改为 `request.post(..., {})`。

### 3.6 第五轮已执行（继续升级）

- **auth**：ban/deleteById 多租户校验。`AuthUserService.ban`、`deleteById` 增加参数 `callerOrganizationId`、`callerIsAdmin`；非 admin 且机构非空时，仅允许操作同机构用户（目标用户 `organizationId` 与调用方一致），否则抛出 FORBIDDEN；Controller 调用时传入 `AuthTokenFilter.getOrganizationId(request)` 与 `isAdmin(request)`。
- **ai**：EvolveScheduler 分布式锁 DB 备用。新增表 `ai_evolve_scheduler_lock`（lock_key, lock_value, expire_at）；当 Redis 不可用时，使用 JdbcTemplate 先删除过期行再 INSERT 抢占锁，执行完毕后 DELETE 释放；锁值使用 `hostname:timestamp` 便于区分实例。

### 3.7 第六轮已执行（继续升级）

- **storage**：统一 POST。`StorageController` 的 `/configured`、`/url` 由 `@GetMapping` 改为 `@PostMapping`（请求体 `/url` 使用 `key` 字段）；前端 `api/storage.ts` 的 `checkConfigured`、`getFileUrl` 改为 `request.post(..., {})`。
- **log**：操作日志 request/response body 采集与脱敏。`OperationLogFilter` 使用 `ContentCachingRequestWrapper`、`ContentCachingResponseWrapper` 包装请求与响应，在 `finally` 中读取缓存 body，经 `BodyMaskUtil.maskAndTruncate` 脱敏（password/token/api_key/secret/authorization 等键值替换为 ***）并截断至 2000 字符后写入 `operationLogService.save`；新增 `log/util/BodyMaskUtil.java`。另修 live 模块既有编译错误：`LiveAiServiceImpl` 中两处 `generateWithLlm` 返回值改为 `.content()`。

### 3.8 第七轮已执行（继续升级）

- **config**：配置版本表与保存时写历史。迁移脚本新增表 `sys_config_version_history`（id, config_id, config_key, old_value, new_value, operator_id, create_time）；新增 Entity `ConfigVersionHistory` 与 `ConfigVersionHistoryRepository`；`ConfigService.save(vo, Long operatorId)` 在更新配置时（oldValue != null）插入一条版本历史；Controller 调用 `save(vo, AuthTokenFilter.getUserId(request))`。
- **config**：`/get` 统一 POST。`ConfigController` 的按 key 获取由 `@GetMapping("/get")` + `@RequestParam` 改为 `@PostMapping("/get")` + `@RequestBody Map<String,String>`（key 从 body 取）；前端未调用该接口，无需改动。

### 3.9 第八轮已执行（继续升级）

- **live**：数据同步接口统一 POST。`LiveDataSyncController` 的 `/session`、`/session/with-compare`、`/product` 由 `@GetMapping` + `@RequestParam sessionId` 改为 `@PostMapping` + `@RequestBody Map<String,Long>`（body 含 `sessionId`）；前端 `api/live.ts` 的 getSessionData、getSessionDataWithCompare、getProductData 改为 `request.post(..., { sessionId })`。
- **product**：销售统计接口统一 POST。`ProductController` 的 `/sales-history/total-sales-amount`、`/sales-history/total-sales-quantity` 由 `@GetMapping` + `@RequestParam productId` 改为 `@PostMapping` + `@RequestBody Map<String,Long>`（body 含 `productId`）；前端 `api/product.ts` 的 totalSalesAmount、totalSalesQuantity 改为 `request.post(..., { productId })`。

### 3.10 第九轮已执行（继续升级）

- **script**：违规词与话术模板接口统一 POST。`ViolationWordAdminController` 的 `/active`、`UserViolationWordController` 的 `/active` 由 `@GetMapping` 改为 `@PostMapping`（请求体可选）；`ScriptTemplateController` 的 `/by-scene` 由 `@GetMapping` + `@RequestParam scene` 改为 `@PostMapping` + `@RequestBody Map<String,String>`（body 含 `scene`）。
- **shortvideo**：话术模板按场景接口统一 POST。`SvScriptTemplateController` 的 `/by-scene` 由 `@GetMapping` + `@RequestParam scene` 改为 `@PostMapping` + `@RequestBody Map<String,String>`（body 含 `scene`）；前端 `api/shortvideo.ts` 的 getScriptTemplatesByScene 改为 `request.post(..., { scene })`。

### 3.11 第十轮已执行（继续升级）

- **product**：风格预设接口统一 POST。`StylePresetController` 的 `/list`、`/list-all` 由 `@GetMapping` 改为 `@PostMapping`（请求体可选）；`GET /{id}` 改为 `@PostMapping("/get")` + `@RequestBody Map<String,Long>`（body 含 `id`）；前端 `api/product.ts` 的 listStylePresets、listAllStylePresets 改为 `request.post(..., {})`。
- **attribution**：归因查询接口统一 POST。`AttributionController` 的 `/session/{sessionId}`、`/summary/{sessionId}` 改为 `@PostMapping("/session")`、`@PostMapping("/summary")` + body 含 `sessionId`；`GET /{id}` 改为 `@PostMapping("/get")` + body 含 `id`；前端 `api/attribution.ts` 的 getBySession、getSummary、getById 改为 `request.post(..., { sessionId })` 或 `request.post('/attribution/get', { id })`。

### 3.12 第十一轮已执行（继续升级）

- **product**：产品话术接口部分统一 POST。`ProductScriptController` 的 `GET /list-by-style/{productId}/{scriptType}`、`GET /active-by-style/{productId}/{scriptType}` 改为 `@PostMapping("/list-by-style")`、`@PostMapping("/active-by-style")` + body 含 `productId`、`scriptType`；`GET /{scriptId}` 改为 `@PostMapping("/get")` + body 含 `scriptId`；`GET /history/{scriptId}` 改为 `@PostMapping("/history")` + body 含 `scriptId`。前端 `api/product.ts` 的 listScriptsByStyle、getActiveScriptsByStyle、listScriptVersionHistory 改为 `request.post(..., { productId, scriptType })` 或 `request.post(..., { scriptId })`；新增 getProductScript(scriptId)。

### 3.13 第十二轮已执行（继续升级）

- **product**：产品话术 list/active 接口统一 POST。`ProductScriptController` 的 `GET /list/{productId}` 改为 `@PostMapping("/list")` + body 含 `productId`；`GET /list/{productId}/{scriptType}` 改为 `@PostMapping("/list-by-type")` + body 含 `productId`、`scriptType`；`GET /active/{productId}` 改为 `@PostMapping("/active")` + body 含 `productId`。前端 `api/product.ts` 新增 listScripts(productId)、listScriptsByType(productId, scriptType)、listActiveScripts(productId)，均使用 `request.post`。

### 3.14 第十三轮已执行（继续升级）

- **auth**：机构接口统一 POST。`OrganizationController` 的 `/my`、`/members`、`/invitations` 由 `@GetMapping` 改为 `@PostMapping`（请求体可选）；`/search-talents` 由 `@GetMapping` + `@RequestParam keyword` 改为 `@PostMapping` + `@RequestBody Map<String,String>`（body 含 `keyword`）。前端 `api/auth.ts` 新增 getMyOrg、getOrgMembers、getOrgInvitations、searchOrgTalents，均使用 `request.post`。
- **douyin**：粉丝画像接口统一 POST。`FanProfileController` 的 `GET /{accountId}` 改为 `@PostMapping("/get")` + body 含 `accountId`；`GET /stats/{accountId}` 改为 `@PostMapping("/stats")` + body 含 `accountId`、`statType`（可选）。前端 `api/douyin.ts` 新增 getFanProfile(accountId)、getFanProfileStats(accountId, statType)。

### 3.15 第十四轮已执行（继续升级）

- **auth**：验证码接口统一 POST。`AuthController` 的 `GET /captcha` 改为 `@PostMapping("/captcha")`，请求体可选。前端 `api/auth.ts` 中 getCaptcha 改为 `request.post('/auth/captcha', {})`。
- **live**：监控快照接口统一 POST 并加鉴权。`LiveMonitorSseController` 的 `GET /stream/{sessionId}/snapshot` 改为 `@PostMapping("/snapshot")`，请求体含 `sessionId`，未登录抛 `BusinessException(UNAUTHORIZED)`，缺 `sessionId` 抛 `PARAM_INVALID`。`LiveMonitorController` 的 `/by-session` 改为从 `@RequestBody Map` 读取 `sessionId`。前端 `api/live.ts` 新增 getMonitorSnapshot(sessionId)、getMonitorBySession 改为 body 传参 `{ sessionId }`。注：`LiveDataSyncServiceImpl` 的 saveSessionData/saveProductData/syncSessionData 已有 `@Transactional`，无需改动。

### 3.16 第十五轮已执行（继续升级）

- **live 数据同步**：`LiveDataSyncController` 的 `/session/sync` 由 `@RequestParam Long sessionId` 改为 `@RequestBody Map` 读取 `sessionId`。前端 `api/live.ts` 中 syncSessionData(sessionId) 改为 `request.post(..., { sessionId })`。
- **live 话术/商品**：`LiveScriptController` 的 `/by-session`、`/effectiveness`、`/save-batch-to-library`、`/export` 均改为从 `@RequestBody Map` 读取 `sessionId`（及 save-batch 的 `scriptIds`），新增 parseSessionId 工具方法。`LiveProductController` 的 `/by-session` 改为从 body 读取 `sessionId`。前端 getProductsBySession、getScriptsBySession、getScriptEffectiveness、saveBatchToLibrary、exportScripts 均改为 body 传参。
- **Resilience4j**：在 `application.yml` 中新增 `resilience4j.timelimiter.instances.liveAiTimeout.timeoutDuration: 30s`，供后续对 Live AI 调用做超时装饰使用。

### 3.17 第十六轮已执行（继续升级）

- **live 分析**：`LiveAnalysisController` 的 `/generate`、`/get`、`/review` 由 `@RequestParam Long sessionId` 改为 `@RequestBody Map` 读取 `sessionId`，新增 parseSessionId。前端 generateAnalysis、getAnalysis 改为 body `{ sessionId }`，新增 getReview(sessionId)。
- **live AI**：`LiveAiController` 的 `/check-similarity`、`/generate-skeleton` 改为从 body 读取 `sessionId`，新增 parseSessionId。前端 checkSimilarity、generateSkeleton 改为 body `{ sessionId }`。
- **live 数据**：`LiveDataSyncController` 的 `/history` 由 `@RequestParam accountId/limit/days` 改为 `@RequestBody Map` 读取 accountId、limit、days（缺 limit 默认 10）。前端 getHistory 改为 `request.post(..., body)` 传参。

### 3.18 第十七轮已执行（继续升级）

- **live 场次**：`LiveSessionController` 的 `/get`、`/delete`、`/overview`、`/readiness`、`/status`、`/viewers`、`/likes` 均由 `@RequestParam` 改为 `@RequestBody Map` 读取参数（id、sessionId、status、viewers、likes 等），新增 parseId(body, key) 工具方法；readiness 支持 body 中 `id` 或 `sessionId`。前端 getSession、getSessionOverview、deleteSession、updateSessionStatus、getReadiness 均改为 body 传参。

### 3.19 第十八轮已执行（继续升级）

- **live 产品/话术**：`LiveProductController` 的 `/get`、`/delete` 改为从 `@RequestBody Map` 读取 `id`，新增 parseId(body)。`LiveScriptController` 的 `/get`、`/delete`、`/executed`、`/save-to-library` 改为从 body 读取 `id`/`scriptId`/`executed`，新增 parseId(body)。前端 deleteLiveProduct、deleteLiveScript、updateScriptExecuted、saveScriptToLibrary 均改为 body 传参。移除两 Controller 未使用的 @Parameter 导入。

### 3.20 第十九轮已执行（继续升级）

- **live 数据同步**：`LiveDataSyncController` 的 `/session/sync-from-douyin` 由 `@RequestParam sessionId/roomId/accessToken` 改为 `@RequestBody Map` 读取 sessionId、roomId、accessToken。前端新增 syncFromDouyin(sessionId, roomId, accessToken)，请求体传参。
- **live AI**：`LiveAiController` 的 `/check-violation`、`/generate-slot`、`/refine-script`、`/chat-for-script` 均由 `@RequestParam` 改为 `@RequestBody Map` 读取 scriptId、requirement、durationLimitSec、question、message 等，新增 parseLong(body, key)。前端 checkViolation、generateForSlot、refineScript、chatForScript 均改为 body 传参。移除未使用的 @Parameter 导入。

### 3.21 第二十轮已执行（继续升级）

- **product**：`ProductController` 的 `/get`、`/delete`、`/infer-product-type`、`/trigger-extract`、`/update-inventory`、`/publish`、`/unpublish`、`/set-featured`、`/sales-history/get` 均由 `@RequestParam` 改为 `@RequestBody Map` 读取 id、productId、quantity、featured 等，新增 parseLong(body, key)。前端 getProduct、deleteProduct、inferProductType、triggerExtract、updateProductInventory、publishProduct、unpublishProduct、setProductFeatured 均改为 body 传参。

### 3.22 第二十一轮已执行（继续升级）

- **copy**：`CopyApprovalController` 的 `/get`、`/delete` 改为从 `@RequestBody Map` 读取 `id`，新增 parseLong。`CopyTemplateController` 的 `/get`、`/delete`、`/update-status` 改为从 body 读取 id、status。前端 getCopyApproval、getCopyTemplate、deleteCopyTemplate、updateCopyTemplateStatus 均改为 body 传参。移除两 Controller 未使用的 @Parameter 导入。

### 3.23 未在本轮自动执行的可选项（后续迭代）
- auth：Redis TokenStore、权限缓存（已做）、多租户 ban/deleteById（已做）。
- ai：GET→POST 统一（部分已做）、EvolveScheduler 分布式锁 DB 备用（已做）、前端 hybridSearch 等。
- live：Resilience4j 重试/超时、N+1 优化、SSE 重连。
- shortvideo：Dashboard 真实数据源、SvProject version 乐观锁、quickGenerate @Transactional。
- config：配置版本表（已做）、敏感值加密。
- storage：断点续传；逻辑删除（SysFile 已有 deleted）。
- log：request_body/response_body 写入与脱敏（已做）。
- dashboard/agent：DashboardService 实现、Agent VO 与 LLM 集成。
- 其余 GET 接口：auth/captcha、OAuth、live/monitor SSE 等仍为 GET，可按需逐步改为 POST。

## 四、验收要点

- [ ] 执行 `upgrade-analysis-2026.sql` 无报错，且表结构/索引符合预期。
- [ ] Live 模块：`LiveMonitor`/`LiveSessionData` 查询均带 `deleted=0`。
- [ ] Product：库存乐观锁冲突返回 3115（INVENTORY_CONFLICT）。
- [ ] Script：非 Admin 调用违规词 CSV 导入/导出返回 403。
- [ ] Copy：非 Admin 执行审批通过/拒绝返回 403。
- [ ] `mvn compile`、`mvn test` 通过。
