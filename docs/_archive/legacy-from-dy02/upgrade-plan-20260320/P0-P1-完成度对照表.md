# upgrade-plan-20260320：P0 + P1 + P2 完成度对照表

> **口径**：收录 `01 / 02 / 03` **§2 差距分析表** 中的 **P0（含 P0-Critical）、P1、P2**。正文 **§4 Backlog** 中与 §2 重复的条目仍以 **§2 编号** 为准，不重复罗列。  
> **图例**：✅ 已达成 | ⚠️ 部分达成 | ❌ 未达成  
> **⚠️ 子类**（避免「部分达成」一锅烩）：**⚠️-MVP** = MVP 已闭合，与理想终局仍有差距；**⚠️-降级** = 规则/启发式/代理指标替代官方 API 或 ML，产品可接受为「规则版终局」或需再立项升级。详见 **§ P0+P1 · ⚠️ 收口计划** 小节前 **类型说明** 与 **收口表** 的 **类型** 列。  
> **说明**：E-6 在 01 表中为 **P0-Critical**，归入 **P0** 计数。  
> **附表三（P2）**：状态为**粗扫**（路径/关键字），精细度低于 P0/P1；便于排期，验收以产品为准。  
> **「全部升级」**（2026-03-20 续）：03 域 **A-5 / V-4 / D-3** 已落 **⚠️ MVP**；**V-2** **xfade** 白名单含 **fade/slide/wipe/圆形/径向/对角** 等（见 §03 P0 **V-2** 行），**单侧无音轨** 用 **anullsrc+atrim** 补静音后 **acrossfade**（双无则铺与成片等长静音轨；失败仍回退 concat）；**V-1** 时间轴「裁切时长」经 **`trimEndSec`/`trimStartSec`** 在 `mergeVideos` 前 **FFmpeg copy 裁剪**；**D-4** 剧本落库时解析 **`【悬念钩子】`** 写入 `cliffhanger`；**N-2** 无 `experimentId` 时 **`recentExperiments`/`recentExperimentTotal`**，下钻统计后响应仍带列表；**N-3** ≥3 条有效 `sv_video` 时 **`derived_from_publish_engagement`**；**ShortVideoDashboardPage** 展示 N-2/N-3；**素材库页**含补元数据/快捷标签；**H-2/H-4** 见 **`hotspot.prompt-extra-max-chars`**、**`effect-predict.system-extra`**；全量多轨 NLE / 复杂调色仍属后续 wave。

---

## 01 最强行业大脑

### P0 / P0-Critical

| ID | 差距摘要 | 状态 | 代码/备注 |
|----|----------|------|-----------|
| S-2 | 个性化排序（认知画像接入） | ✅ | `KbHybridRetrieveServiceImpl`、`app.ai.search.personalization` |
| E-1 | 自适应进化调度 | ✅ | `EvolveRoiServiceImpl`、`EvolveScheduler`、`app.ai.evolve.adaptive` |
| E-3 | 质量反馈闭环 | ✅ | **知识库文档**：`KbDocumentQualityService`（检索反馈 `markTier` + `KbDocumentQualityScheduler` 启发式扫描）；`AiKbDocument.quality_tier/heuristic_score`（**Flyway V077**）；`KbHybridRetrieveServiceImpl` 对 `quality_tier=2` `retrieve-penalty`；与 `KnowledgeBoostService` 并存。（`KnowledgeQualityScoringService` 仍为 **直播话术版本** 周期分，域不同） |
| E-6 | 进化并发安全（P0-Critical） | ✅ | `EvolveDocumentLockService`、`EvolveEngineServiceImpl`；调度锁 `EvolveScheduler` / `DeepEvolveScheduler` |
| T-4 | 工具错误恢复 | ✅ | `LlmToolOrchestratorService.executeWithRetry`、失败 JSON 不中断编排 |
| T-6 | 工具限流 | ✅ | `ToolRateLimiterService` + `tryAcquire` |
| G-1 | 实体消歧 | ✅ | `IndustryKnowledgeGraphServiceImpl` TF‑IDF 消歧 |

**01 · P0 计数**：✅ 7 · ⚠️ 0 · ❌ 0（条目 7）

### P1

| ID | 差距摘要 | 状态 | 代码/备注 |
|----|----------|------|-----------|
| S-1 | HyDE 假设文档嵌入 | ✅ | `HydeExpansionServiceImpl` + `KbHybridRetrieveServiceImpl#resolveVectorWithOptionalHyde`（Milvus：`AI_SEARCH_HYDE_ENABLED` / `app.ai.search.hyde`；向量融合查询 embedding + 假设段落 embedding；ES 仍为原 `q`）；模型链 `kb_hyde`→`query_rewrite`；`SearchMetricsCollector` `totalHydeUsed` / `ai.search.hyde_used`；Flyway **V076** |
| S-3 | 同义词/语义扩展 | ✅ | `QuerySynonymExpansionServiceImpl`：`app.ai.search.synonyms` 可配置词组（默认美护成分/肤质等价词）；`KnowledgeBaseServiceImpl` 在 `query_rewrite` 之后注入变体，参与混合检索；**非**独立 DB 词表（可后续迁 `sys_config` / 运营表） |
| S-5 | Reranker 回退不完整 | ✅ | `RerankerServiceImpl`：主 `provider`（`cohere` \| `custom` \| `ollama` `/api/rerank`）失败后依次尝试 **Ollama → custom URL → Cohere**（跳过已作为主通道的项），再 **`fallback-to-llm`**，最后 **embedding 余弦**；有效分数须与候选条数一致；Prometheus `ai.rerank.tier`（`primary_*` / `ollama_rerank` / `custom` / `cohere` / `llm` / `embedding_cosine` / `failed`） |
| E-2 | 多角度系统编排 | ✅ | `EvolveEngineServiceImpl#pickEvolveAngle`：`app.ai.evolve.angle-strategy` = **round_robin**（`countByKbId` 取模内置 11 角）/ **least_used_recent**（近 80 条 `evolve_angle` 频次最低优先）/ **random**（沿用 `ThreadLocalRandom`）；手工 `evolveAngleHint` 仍优先生效 |
| E-5 | 分角度 ROI | ✅ | `EvolveRoiServiceImpl#getRoiMetrics` 增加 `byEvolveAngle`（与最近 N 轮任务窗口一致）：每角 `taskCount`、`avgScore`、`indexedLikeCount`（score≥50）、`storageRate`；无角度任务不计入该列表 |
| T-2 | 工具输出 Schema 校验 | ✅ | `LlmRegisteredTool.outputJsonSchema`、`LlmToolOrchestratorService.validateToolOutput` |
| T-3 | 工具结果缓存 | ✅ | `LlmToolOrchestratorService`：`ai:llm-tool:result:v1:{sha256}`、`result-cache.*`；参数 JSON 键递归排序；超大响应跳过写入 |
| T-5 | 工具 Micrometer 监控 | ✅ | `LlmToolOrchestratorService`：`ai.llm_tool.orchestration.(total|duration)`（outcome、error_type）；`ai.llm_tool.invocations` / `ai.llm_tool.latency`（tool、result：success、cache_hit、failure 等）；`ai.llm_tool.retries_recovered`；无 `MeterRegistry` 时 no-op |
| G-3 | 图谱多跳遍历 | ✅ | `IndustryKnowledgeGraphServiceImpl`：`app.ai.brain.knowledge-graph.max-hops`（1–4，默认 2）、`max-edges-per-node`；JPA `getGraphContextForQuery` DFS 路径；Neo4j `getEntityDetail` `*1..h` |
| G-4 | 图谱新鲜度追踪 | ✅ | `IndustryKnowledgeGraphServiceImpl#getGraphContextForQuery`：DFS 触及实体的 `update_time` 聚合为 `图谱新鲜度：X～Y 天前更新`；`app.ai.brain.knowledge-graph.freshness-hint`；无单独 `lastUpdated` 字段名（沿用 `update_time`） |
| F-1 | 搜索自动补全 | ✅ | `searchKnowledgeSuggest` + `KnowledgeSearchPage` 建议芯片（日志热词 + 静态兜底）；`.search-suggestions` |
| F-3 | 分面搜索 | ✅ | `KbSearchFilters` 传 `KbSearchVO`；页面 Accordion：来源类型 / script_type / 创建起止 |
| F-5 | 搜索指标仪表盘 | ✅ | `AiAdminInfraServiceImpl#getSearchStats` 合并 `SearchMetricsCollector` + `ai_search_log`（24h `countSince`/`countZeroHit`/`findTopQueriesSince`）；`MonitoringPage` 卡片「检索业务指标」+ 导出列；管理员 `POST /ai/admin/infra/search/stats` |
| B-1 | GrowthPath 去模板化 | ✅ | `app.ai.brain.growth-path.phase-milestone-fans`（`AI_BRAIN_GROWTH_MILESTONES`）；`GrowthPathServiceImpl` 战略阶段与兜底三阶段均用解析后的里程碑；非法配置回退默认 |
| B-2 | FiveHostsSynergy 配置化 | ✅ | `app.ai.brain.synergy.*`：`flow-path-description`、`transfer-triggers/channels`（CSV）、`expected-transfer-rate`、`transfer-hint`；`FiveHostsSynergyServiceImpl` 空配置时用代码内与历史一致的默认文案 |

**01 · P1 计数**：✅ 15 · ⚠️ 0 · ❌ 0（条目 15）

---

## 02 最强直播话术

### P0

| ID | 差距摘要 | 状态 | 代码/备注 |
|----|----------|------|-----------|
| L-1 | 评分权重配置化 | ✅ | `LiveEffectivenessConfigService` + `EffectivenessScoreServiceImpl` |
| L-2 | 真实转化率 | ✅ | 订单/峰值观众等路径；无订单时 `productImpressions/peakViewers` 仍偏代理指标 |
| L-3 | 真实完成率 | ✅ | **规则版（ADR 006）**：**`LiveOfficialCompletionRateProvider`** 优先、无则回退双点推算（`EffectivenessScoreServiceImpl`）；**completionHint** / [`05-直播完播推算与监控时序.md`](../development/05-直播完播推算与监控时序.md)；官方会话完播字段仍待开放平台，**UI 须**区分 **official_proxy** / **monitor_derived** |
| L-8 | 定时评分更新 | ✅ | `@Scheduled` + `app.live.effectiveness.schedule` |
| R-1 | 抖音 API 实时同步 | ✅ | `DouyinDataCollector` → `ingestDouyinLiveMetrics` 写 `live_session_realtime_data`；`LiveRealtimeSseHub.broadcastDataUpdate`；需 `DOUYIN_COLLECTOR_ENABLED=true` + OAuth |
| Q-1 | 自动质量评分 | ✅ | `LiveScriptQualityScoringServiceImpl` 多维 + `live_script_quality_score` |

**02 · P0 计数**：✅ 6 · ⚠️ 0 · ❌ 0（条目 6）

### P1

| ID | 差距摘要 | 状态 | 代码/备注 |
|----|----------|------|-----------|
| L-4 | 时间衰减因子 | ✅ | `EffectivenessScoreServiceImpl`：`update_time` 锚点 × 半衰期缩放 `totalScore`；`app.live.effectiveness.time-decay.*` |
| L-6 | 置信区间 / 小样本 | ✅ | `executed` 为样本量：`low-sample.threshold` 以下抑制 `hot/recommend/emerging`，可选 `demote-in-ranking`；API `confidenceLow` |
| R-2 | 建议引擎深度 | ✅ | `LiveRealtimeSuggestionServiceImpl`：留存/互动/转化阈值规则 |
| R-3 | 倒计时持久化 | ✅ | `LivePanelTimerRedisService` + `POST .../timer-state/save|load`；`LivePromptDisplay` 恢复/防抖同步；`app.live.realtime-panel.timer` |
| R-5 | 【】表演指导高亮 | ✅ | `performanceCueText` + `ScriptContentWithPerformanceCues`：与质量检测一致 `【` 内 2–30 字；`LiveRealtimePanel` / `LivePromptDisplay` 内联高亮 + 芯片条；`ScriptSection` 预览内联高亮 |
| R-7 | SSE 自动重连 | ✅ | `subscribeWithRetry`：默认 **5 次**、**指数退避**（上限 30s） |
| Q-2 | 抄袭检测 | ✅ | `LiveScriptQualityService#checkCorpusDuplicate` / `mergeCorpusDuplicateHint` / `applyGenerationQualityHints`：同用户近期 `live_script` 正文 **bigram Jaccard**；`LiveAiResultVO.duplicateCheck`；`ai_suggestion` `[查重]`；**非** Milvus/ES 向量指纹（可后续增强） |
| Q-4 | 时长/节奏校验 | ✅ | `LiveScriptQualityServiceImpl.checkDurationFit` |
| Q-6 | 质量分持久化 | ✅ | `LiveScriptQualityScore` 实体与仓库 |
| C-1 | 竞品账号追踪 | ⚠️ | **短视频**：`/api/v1/short-video/competitor/*`；**直播同源桥**：**`POST /api/v1/live/competitor-monitor/list`**；**`AdminLayout`/`OrgLayout`/`TalentLayout`** → **`/shortvideo/competitor`**；口径 [`02-直播话术-竞品口径说明.md`](./02-直播话术-竞品口径说明.md)；话术/场次维深度关联仍续 |
| C-4 | 竞品话术库 | ✅ | `live_competitor_script` + **Flyway V078**；`LiveCompetitorScriptController` `/api/v1/live/competitor-script`（search/get/save/delete）；owner 隔离 |
| M-1 | 自定义场次模板 | ✅ | `live_session_template` + V078；`LiveSessionTemplateController` `/api/v1/live/session-template`；`LiveSession.templateId` + `LiveSessionSaveVO`/`VO` |
| M-2 | 动态槽位分配 | ✅ | `LiveScriptServiceImpl`：空槽位且 `templateId>0` 时 `LiveSessionSlotBlueprintParser` + `ensureScriptSlotsFromBlueprint`；`chat_2h` 仍 14 槽但商品 ID 同步走 `syncProductIdsToSlots` |
| M-5 | 槽位时长校验（生成侧） | ✅ | `LiveScriptQualityService#mergeDurationFitHint`：同步槽位生成 / 商品话术直出 / `LiveScriptStreamServiceImpl` 流式落库 / `analyzeAndRefineFullScript` 成篇保存前写入；`LiveAiResultVO.durationFit` + `ai_suggestion` 前缀 `[时长]`；**不**改 `generation_status` |
| M-7 | 商品不足降级 | ✅ | `LiveScriptServiceImpl.handleProductShortage`：可配置策略（`repeat_main`/`convert_to_chat`/`merge_adjacent`）；`app.live.slot.product-shortage-strategy`；`ai_suggestion` 提示运营 |
| M-8 | 聊天话题库 | ✅ | `app.live.generation.chat-topic-pool`；`LivePromptBuilder` 聊家常槽位注入「结构化话题参考」（按 `slotIndex` 轮换；无配置用内置池） |
| G-1 | 多商品批量并行生成 | ✅ | `POST /api/v1/live/ai/generate-parallel`；`LiveScriptGenerationServiceImpl.generateParallel`（虚拟线程/Executor 并行，并发上限 `app.live.generation.parallel.max-concurrency`，超时 `timeout-seconds`）；部分失败不拖死全场 |
| G-3 | Prompt 版本追踪 | ✅ | `generation_prompt_hash`：`generateWithLlm` 与 `LiveScriptStreamServiceImpl`（`generate-slot-sse` + `AccumulatingSseOutputStream` / `ModelChatStreamService` 成功回调）一致算法；失败事件不回调不落库 |
| G-4 | A/B 生成策略 | ✅ | `ScriptStyleAbService.assignStyle`（`experiment_type=script_style`、`target_entity_type=live_session`）；**同步**：`LiveScriptGenerationServiceImpl#tryAssignAbStyle` 写 `style` + `ab_experiment_id` / `ab_variant_id`；**流式**：`LiveScriptStreamServiceImpl` 与同会话指纹 `session_{id}` 对齐，落库同上（2026-03-20 补齐）；与通用 `AbTestService` 事件打点并存 |

**02 · P1 计数**：✅ 18 · ⚠️ 1 · ❌ 0（条目 19）

---

## 03 最强短视频

### P0

| ID | 差距摘要 | 状态 | 代码/备注 |
|----|----------|------|-----------|
| V-1 | 时间线编辑器 | ⚠️ | `VideoTimeline` 拖拽排序 + **片头/片尾** 裁切（**MUI Dialog + Slider** 调 `trimStartSec`/`trimEndSec`）→ `VideoEditingPage` `auto-compose` `materials.videos[]` → `ClipTrim` **合并前逐段 copy 裁剪**；**待续**：完整多轨 NLE |
| V-2 | 转场特效 | ⚠️ | **xfade** 白名单：`fade`、`slideleft`/`slideright`/`slideup`/`slidedown`、`wipeleft`/`wiperight`/`wipeup`/`wipedown`、`circleopen`/`circleclose`、`radial`、`diagtl`/`diagtr`/`diagbl`/`diagbr`；`VideoEditingPage` 可选；音轨策略同前；`cut`/`none` **concat**；xfade 失败回退 concat。**非**全库电影转场包 |
| V-3 | 字幕/文字轨道 | ⚠️ | `generate-subtitles` 返回 **`srt`/`srtUrl`**；**`auto-compose`**：`materials.subtitles` **优先**，否则 **`srt`/`srtUrl`** 解析烧录；**`burnSubtitles`**；`VideoEditingPage` 成片烧录开关；**`WorkflowExecutionService#runComposeStep`** 经 **`mergeWorkflowComposeMaterials` + `AutoComposeSubtitleResolver`** 与 `auto-compose` **同源优先级**。**非**独立多轨字幕编辑器 |
| D-1 | 多集批量生成 | ✅ | `POST .../drama/generate-all-episodes-script-sse` + `DramaServiceImpl#generateDramaScript(..., progress)`；前端 `generateDramaScriptAllEpisodesSse`；**一次 LLM 全剧**后按分隔符写入各集 `synopsis` 并推送 `episode_done/complete`。**非**逐集独立 LLM 对话。**剧集→项目**：`POST .../drama/create-projects-from-episodes-batch`（≤30，`successes`/`failures`）；`DramaEditorPage`「批量建项」 |
| A-2 | 素材标签/分类 | ⚠️ | 列表 **`POST .../library/list`**：**`tags[]`** OR + **`taxonomyCodes[]`**（与 `sys_taxonomy_node` **V092** 解析）；**`POST .../system/taxonomy/list`**；`tag-options` 含 **`taxonomy`**；**live/product** 筛选共用 taxonomy 待排期 |
| T-1 | 死信队列（§2.6） | ✅ | `VideoGenerationTaskAmqpConfig` DLX/DLQ |
| T-3 | 任务超时（§2.6） | ✅ | 超时调度 + `app.shortvideo.video-task` |
| K-1 | 自动排期 | ✅ | `POST .../content/calendar-auto-schedule`；`ContentCalendarServiceImpl#autoScheduleByRecommendation` 按推荐写入 `scheduleDate`/`publishTime` |

*注：03 §2.6 的 T-1/T-3 与 01 §2.3 工具条目代号相同，以章节区分为准。*

**03 · P0 计数**：✅ 4 · ⚠️ 4 · ❌ 0（条目 8）

### P1

| ID | 差距摘要 | 状态 | 代码/备注 |
|----|----------|------|-----------|
| V-4 | 滤镜/调色 | ⚠️ | **`colorGamma`** + 可选 **`colorUnsharpAmount`**（eq 后链式 **unsharp**）；`VideoEditingPage` / `auto-compose` / 工作流透传；**未**加 vibrance/hue 等旋钮（见 `docs/development/04-文档代码同步清单.md` §八）；**非** LUT 管线 |
| V-6 | 多轨道编辑 | ⚠️ | `VideoTimeline` 多段视频 + **`VideoEditingPage` 同步 BGM/配音轨标签**（`bgmUrl`/`audioTrack`）；**非**独立多轨剪辑器 |
| D-2 | 角色一致性校验 | ✅ | **规则版（ADR 006）**：`PersonaConsistencyCheckerImpl`：JSON 字段说明（score/禁改全文）；**`persona-consistency.system-extra`/`user-extra`**；**`max-generated-chars`** 截断；**非**跨集视觉模型 |
| D-3 | 剧情连贯性 | ⚠️ | 集号断层、`openingCliffRecall`、单集角色、**`cliffhangerDirty`**、**`nextSynopsisTooShort`**；`DramaEditorPage` **Accordion** 折叠详情 + **定位第 N 集** 滚动；**D-4** `DramaController#extractCliffhangerCarryover` POST/SSE 共用 |
| D-4 | 悬念/钩子生效 | ⚠️ | 末行 **`【悬念钩子】`** + 解析入库 `cliffhanger`；**生成侧**：`cliffhanger-carryover` + **`script-system-extra`/`script-user-extra`**；`POST .../generate-script` 与 **SSE** 同走 **`generateDramaScript`**；`DramaEditorPage`「悬念承接」；**非**逐集独立对话自动改稿 |
| A-1 | 素材智能推荐 | ✅ | `POST .../library/recommend-by-script`；`MaterialLibraryServiceImpl#recommendMaterialsByScript`：`VectorService` 余弦 + 失败回退关键词 |
| A-3 | 使用分析 | ✅ | `POST .../library/usage-stats`：`sameUrlCopies`、`distinctProjectCount` 等 |
| A-4 | 批量操作 | ✅ | `POST .../library/batch-update-tags`；`MaterialLibraryPage` 批量打标对话框 |
| A-5 | 缩略图/元数据自动提取 | ⚠️ | `enrich-retry-*`、单条富化指标；**`refresh-metadata-batch`** **`failures`** + **`reasonBucket`**（**`MaterialRefreshFailureClassifier`** 统一批量/富化 bucket）；**`shortvideo.material.refresh_batch`** + `log.warn`；`MaterialLibraryPage` Toast。**依赖** 不变 |
| K-2 | 拖拽排期 | ✅ | `POST .../content/update-schedule-date`；`ContentCalendarPage` 日详情弹窗 **改期**（选新 `scheduleDate`） |
| K-3 | 多账号排期 | ✅ | `contentCalendar` / `contentCalendarStats` 传 **`accountIds`**；`ContentCalendarPage` **多选抖音账号**筛选日历与统计 |
| K-4 | 冲突检测 | ✅ | `POST .../content/schedule-conflicts`；`detectPublishConflicts`（默认同账号 30 分钟内） |
| K-6 | 批量排期 | ✅ | `POST .../content/batch-schedule-sequential`；按天顺序 + 固定 12:00 |
| N-1 | 队列/分群分析 | ✅ | `POST .../dashboard/cohort-analysis`：`byProjectType`、`plannedPublishByHourSlot` |
| N-2 | A/B 测试分析 | ✅ | **规则版（ADR 006）**：**`statistics`** + **`POST .../abtest/event/list`**；无 `experimentId` 时 **`recentExperiments`**；深化漏斗另排期 |
| N-3 | 受众画像 | ✅ | **规则版（ADR 006）**：**derived** / **placeholder** + **`import-csv`**（**`sv_audience_profile_import`** **V093**）；官方画像可选增强 |
| N-4 | 内容效果关联 | ✅ | `POST .../dashboard/effect-correlation`：时长分桶 vs 平均互动率（点赞+评论/播放） |
| T-2 | 任务优先级（§2.6） | ✅ | `VideoGenerationTaskAmqpConfig`：`x-max-priority`（`app.shortvideo.video-task.amqp.max-priority`，**0=不声明**以便旧队列兼容）；`POST .../video-task/submit`  body `mqPriority` 0–10；重试保留 `requestJson.mqPriority` |
| T-4 | 任务批处理（§2.6） | ✅ | `POST .../video-task/submit-batch`（最多 20 条）；`VideoGenerationTaskServiceImpl#submitTasksBatch` |
| H-2 | HotspotWindowService | ✅ | **规则版（ADR 006）**：分档 + **`prompt-extra`**；**`recency-window-days`** + **`hotspot-window`** **`recencySignals`**；**非** Prophet |
| H-3 | PersonaConsistencyChecker | ✅ | **规则版（ADR 006）**：同 D-2；**`persona-consistency.*`** |
| H-4 | ContentEffectPredictor | ✅ | **规则版（ADR 006）**：**`calibration-factor`/`calibration-note`** + **`confidence`**；**非**统计回归 |

**03 · P1 计数**：✅ 17 · ⚠️ 5 · ❌ 0（条目 22）

---

## 总汇总

| 文档 | 档位 | ✅ | ⚠️ | ❌ | 条数 |
|------|------|----|----|-----|------|
| 01 大脑 | P0 | 7 | 0 | 0 | 7 |
| 01 大脑 | P1 | 15 | 0 | 0 | 15 |
| 02 直播 | P0 | 6 | 0 | 0 | 6 |
| 02 直播 | P1 | 18 | 1 | 0 | 19 |
| 03 短片 | P0 | 4 | 4 | 0 | 8 |
| 03 短片 | P1 | 17 | 5 | 0 | 22 |
| **合计** | **P0** | **17** | **4** | **0** | **21** |
| **合计** | **P1** | **50** | **6** | **0** | **56** |
| **合计** | **P0+P1** | **67** | **10** | **0** | **77** |

**粗完成率**：

- **P0**：完全达成 **17/21 ≈ 81.0%**（**ADR 006** 后 L-3 计 ✅ 规则版）；含 ⚠️ 加权（各计 0.5）约 **90.5%**。  
- **P0+P1**：完全达成 **67/77 ≈ 87.0%**；⚠️ 计 0.5 时约 **93.5%**（P1 无剩余 ❌ 项）。  
- **说明（2026-03-22）**：7 条原 **⚠️-降级**（L-3/D-2/N-2/N-3/H-2/H-3/H-4）经 [**ADR 006**](../adr/006-P0P1-规则版终局指标.md) 翻 ✅；剩余 **P0+P1 共 10 条 ⚠️** 均为 **⚠️-MVP**，与 P2 五条 ⚠️ 合计见 **§ 全部 ⚠️ 一览**。「逐条全部升级」共 **108** 项，本表随迭代更新 ✅/⚠️/❌。

---

## P0+P1 · ⚠️ 收口计划

> **已闭合（规则版）**：7 条原 **⚠️-降级** 于 2026-03-22 经 [**ADR 006**](../adr/006-P0P1-规则版终局指标.md) 纳为终局口径，主表已标 **✅（规则版）**，**不再占用**下表排期。  
> **仍待指派**：以下 **10 条** 均为 **⚠️-MVP**（能力边界/跨模块挂靠）；**体量** 供排期参考（S≈≤3d / M≈1–2wk / L≈多 Sprint）。Owner、目标日期仍填 `待指派` / `TBD`，例会替换。  
> **与 P2**：全库 **⚠️** 见 **§ 全部 ⚠️ 一览**（**15** = 本表 10 + P2 附表三 **5**）。

### ⚠️ 类型说明

| 类型 | 含义 |
|------|------|
| ⚠️-MVP | MVP 已交付；终局能力（多轨 NLE、LUT…）另排 Epic 或 **ADR** 标「白名单版」终局。 |
| ⚠️-降级 | 规则/启发式/代理；若接受为终局 → **ADR** + 主表 **✅（规则版）**（已采纳见 **ADR 006**）。 |

### 已 ADR 接受 · ✅ 规则版（不占收口表）

| ID | 文档 |
|----|------|
| L-3 / D-2 / N-2 / N-3 / H-2 / H-3 / H-4 | [`006-P0P1-规则版终局指标.md`](../adr/006-P0P1-规则版终局指标.md) |

### 待指派 · ⚠️-MVP 收口表（10 条）

| ID | 体量 | 当前状态摘要 | 收口方案 | Owner | 目标日期 | 验收要点 |
|----|------|--------------|----------|-------|----------|----------|
| C-1 | M | 竞品在短视频域；直播侧 `competitor-monitor/list` 桥接 | 话术场次关联竞品 **或** 主表 ✅+「短视频域」 | 待指派 | TBD | [`02-直播话术-竞品口径说明.md`](./02-直播话术-竞品口径说明.md) |
| V-1 | L | 时间线 + 裁切 | 多轨 NLE Epic 或里程碑 | 待指派 | TBD | trim → `mergeVideos` 前裁剪 E2E |
| V-2 | M | xfade 白名单 | 扩展包 **或** ADR 白名单终局 | 待指派 | TBD | 前后端白名单、非法 `warn` |
| V-3 | L | 字幕烧录链 | 多轨字幕轨 **或** ADR burn 终局 | 待指派 | TBD | compose/工作流/剪辑同源单测 |
| A-2 | M | tags + taxonomy V092 | live/product 共用 taxonomy（`04` §八） | 待指派 | TBD | 与 `sys_taxonomy_node` 一致 |
| V-4 | L | gamma + unsharp | LUT **或** ADR 非 LUT | 待指派 | TBD | [`V-4`](../modules/shortvideo/V-4-成片调色与FFmpeg约束.md) |
| V-6 | L | 多段 + BGM/配音展示 | 独立多轨 **或** ADR Timeline 边界 | 待指派 | TBD | 与 BgmPanel/`voiceClipUrls` 一致 |
| D-3 | M | 连贯 + UI | 自动改稿 **或** 启发式 ADR | 待指派 | TBD | 风险芯片可复现 |
| D-4 | M | 悬念 + carryover | 逐集改稿 **或** ADR 单提示链 | 待指派 | TBD | POST/SSE、`cliffhanger` 一致 |
| A-5 | M | 批量富化 + bucket | MQ/BOS 等 | 待指派 | TBD | `refresh_batch` + Toast/导出 |

---

## 附表三：P2（§2 差距表）

### 01 最强行业大脑 · P2

| ID | 差距摘要 | 状态 | 代码/备注（粗扫） |
|----|----------|------|-------------------|
| S-4 | 查询意图分类（how-to/定义/对比） | ✅ | `QueryIntentClassifier` + `app.ai.search.intent.*` + `KbHybridRetrieve`/`KnowledgeBaseController`（Epic-P2-01） |
| E-4 | 跨进化依赖 / 多阶段 DAG | ✅ | **V082** `depends_on_task_nos`/`blocked_reason`；`EvolveEngineService` blocked + `EvolveTaskCompletedEvent` 解锁；`trigger` body `dependsOnTaskNos` |
| T-1 | 工具依赖图（§2.3） | ✅ | `LlmRegisteredTool#prerequisiteToolNames` + `LlmToolOrchestratorService` 拓扑重排 WARN；**与 03 §2.6 T-* 不同编号体系** |
| G-2 | 关系类型推理 | ⚠️ | `co_mentioned` 推断边 + **`ai_graph_relation_suggestion`**（**V093**）+ **`.../knowledge-graph/relation-suggestions/list`**；规则学习/弱监督仍待增量 |
| G-5 | 结构化子图 JSON 返回 | ✅ | `IndustryKnowledgeGraphService#getGraphJsonForQuery`；`POST .../ai/brain/knowledge-graph/subgraph-json` |
| G-6 | 图谱矛盾关系校验 | ✅ | `graphJson.contradictions`：compete/partner 互斥边检测（非推断边） |
| F-2 | 结果解释（命中词/源/权重） | ✅ | `SearchResult.explain`（RRF 榜位/分量/通道/重排）；`KnowledgeSearchPage` Accordion |
| F-4 | 进化任务 DAG 可视化 | ✅ | `EvolutionTasksPage` + dagre + `@xyflow/react`（依赖 `dependsOnTaskNos`） |
| F-6 | 进化 ROI 可视化 | ✅ | `POST .../admin/evolve/roi-series` + `EvolutionTasksPage` ECharts 时序 |

**01 · P2 计数**：✅ 8 · ⚠️ 1 · ❌ 0（条目 9）

### 02 最强直播话术 · P2

| ID | 差距摘要 | 状态 | 代码/备注（粗扫） |
|----|----------|------|-------------------|
| L-5 | 分段评分 | ✅ | **V084** `live_script_effectiveness` 段分 JSON；`EffectivenessScoreServiceImpl` 按 `\n\n` 切段写入 |
| L-7 | 评分异常检测 | ✅ | 同上表相对波动 + `app.live.effectiveness.anomaly.*` 标记 |
| R-4 | 演讲备注 / 提词卡 | ✅ | **`presenter_notes`** + Flyway **V083**；提词/详情 UI；`LiveRealtimePanel` slot 对齐 |
| R-6 | 竞品实时指标并排 | ✅ | **`POST .../live/realtime-panel/multi-session-metrics`** |
| Q-3 | 情感分析 | ✅ | 词典情感进 `LiveScriptQualityServiceImpl`（MVP） |
| Q-5 | 表演指导 NLP 理解 | ⚠️ | 同上 + **`performanceCueStrategyPlan`**（分级：规则→词典→可选 LLM）；`app.live.generation.performance-cue-llm.*` 默认关 |
| C-2 | 竞品定价分析 | ✅ | **`live_competitive_insight`** **V085** + CRUD；手工价（MVP） |
| C-3 | 市场份额 / 竞品 GMV | ✅ | 同上表 `market_share_percent` / `gmv_estimate` |
| C-5 | 胜负分析 | ✅ | 同上 `win_loss_notes` |
| M-3 | 时段商品轮换 | ✅ | **`slot_rotation_mode`** **V086** + `LiveScriptServiceImpl.applySlotStrategies` / `hourly_block` |
| M-4 | 热度时段优化槽位时间 | ✅ | **`heat_reorder_enabled`** + 绑品按爆品/利润品优先 |
| M-6 | 模板版本 / 模板 A/B | ✅ | **`session_template`** 实验（Flyway **V089**）；`SessionTemplateAbServiceImpl`；新建场次未指定模板时 AB 分配 **`templateId` + `template_ab_tag`**（`LiveSessionServiceImpl`） |
| G-2 | 长时间生成异步队列 | ✅ | **`app.live.generation.async.*`** + **`live_generation_task`** + MQ（Epic-P2-02 波次 A） |

**02 · P2 计数**：✅ 12 · ⚠️ 1 · ❌ 0（条目 13；仅 **Q-5** 为 ⚠️）

### 03 最强短视频 · P2

| ID | 差距摘要 | 状态 | 代码/备注（粗扫） |
|----|----------|------|-------------------|
| V-5 | 关键帧动画 / 属性时间轴 | ✅ | Flyway **V088** `sv_shot.trim_*`；**`clipTrims`**：`WorkflowExecutionService` / **`ShortVideoEditController` auto-compose**；前端 **`VideoEditingPage`** 持久化 |
| D-5 | 短剧模板库 | ✅ | 表 **`sv_story_formula_template`**（**V090**）；`.../drama/story-formula-template/{save|delete}` + 列表合并预置；**`DramaEditorPage`** |
| A-6 | 素材内容去重 | ✅ | **dHash/pHash** 写入 `perceptual_hash`；**`MaterialLibraryServiceImpl`** 列表 **`nearDuplicate*`** |
| K-5 | 日历侧触达/互动预测 | ⚠️ | **`calendar-forecast`**：`heuristic` + **`statistical_v1/v2`**；**`featureVersion`**、**`historicalVideoCountInScope`**、可选 **`mlExtensionHint`**；非 ML |
| N-5 | ML 预测分析 | ⚠️ | **`ops-dashboard-summary`**：`rulesSummary` + **`opsPanel`**（多指标+关联 API）；**`ops-report-export`** CSV；真 ML 另迭代 |
| N-6 | 自定义报表 | ✅ | **`sv_ops_report_template`**（**V091**）；**`.../content/ops-report-template/{list|save|delete}`**；摘要读 DB；**`OpsReportScheduleJob`**（`app.shortvideo.ops-report.*`，默认关） |
| T-5 | Webhook 通知 | ✅ | DLQ list + **`POST .../video-task/webhook-dlq/replay`** + 素材页「重放通知」 |
| H-1 | 电影级知识库 | ⚠️ | **工程已交付**：**`CinematicOpsController`** — `scene-camera-mapping/{list|save|delete}`（admin 写）、**`generation-log/summary`**；**`logGeneration`** + **Milvus** 时 RRF 见 `CinematicKnowledgeService`。**终局仍缺**：LUT/镜头本体/多模态检索见 **`H-1-电影级知识库差距.md`** |
| H-5 | 工作流引擎 | ✅ | **musicGen/sfxGen/digitalHuman** 对接 **`AiMusic`/`Sfx`/`DigitalHumanSynthesisService`** + BOS；**`app.shortvideo.digital-human`**；占位开关仍可用 |

**03 · P2 计数**：✅ 6 · ⚠️ 3 · ❌ 0（条目 9）

### 附表三 · P2 ⚠️ 条目验收标准（建议）

> 供产品/研发 closure 使用；达标后可改 ⚠️→✅ 或 ADR 接受「规则版终局」。数值阈值可在首批真实数据后修订。

| ID | 建议验收标准 |
|----|----------------|
| G-2 | 至少 **3** 个行业租户下图谱节点 **>**50、关键实体类型边覆盖率（相对可连边上限）**>**60%（或**相对基线 +20%**，取团队定义）**；`relation-suggestions` 审核 API 可用率 **100%**（无 5xx）。 |
| Q-5 | 开启 `performance-cue-llm` 时：缓存/配额/防抖不拖垮生成 P95；**或** 关闭 LLM 时规则路径：`LiveScriptQualityServiceImplPerformanceCueParseTest` 全绿 + 运营抽样「提示有用」**≥70%**（无样本前以单测+代码审查替代）。 |
| H-1 | 运营面：**常用拍摄场景 ≥20** 条在 `scene-camera-mapping` 有映射且无互斥校验错误；`generation-log/summary` 可检索；终局 LUT/镜头本体仍按 [`H-1-电影级知识库差距.md`](../modules/shortvideo/H-1-电影级知识库差距.md) 排期。 |
| K-5 | `calendar-forecast`：在 **historicalVideoCountInScope ≥ N**（建议 **10**）的账号上，预测 vs 实际互动的 **MAPE <30%**（指标定义见 API 文档）；`featureVersion` 变更写入响应便于回放。 |
| N-5 | **每日**（或配置的 `cron`）**`ops-dashboard-summary`/`ops-report-export` 可生成**；报表数据相对源库延迟 **<1h**（以 DB `update_time` 与导出时间戳核对）；`rulesSummary`/`opsPanel` 字段非空率 **100%**。 |

### P2 小计

| 文档 | ✅ | ⚠️ | ❌ | 条数 |
|------|----|----|-----|------|
| 01 大脑 | 8 | 1 | 0 | 9 |
| 02 直播 | 12 | 1 | 0 | 13 |
| 03 短片 | 6 | 3 | 0 | 9 |
| **合计** | **26** | **5** | **0** | **31** |

---

## 全档位汇总（P0 + P1 + P2）

| 档位 | ✅ | ⚠️ | ❌ | 条数 |
|------|----|----|-----|------|
| P0 | 17 | 4 | 0 | 21 |
| P1 | 50 | 6 | 0 | 56 |
| P2 | 26 | 5 | 0 | 31 |
| **合计** | **93** | **15** | **0** | **108** |

**说明**：P2 合计 **31** 条（9+13+9）；全表 **108** = 77（P0+P1）+ 31（P2）。**2026-03-22**：**ADR 006** 将 P0+P1 中 **7** 条由 ⚠️ 翻 ✅（规则版），故全表 ⚠️ 自 **22** 减至 **15**，✅ 自 **86** 增至 **93**。

---

## 全部 ⚠️ 一览（15 条 · 统一视图）

> **用途**：一张表看见仍标 **⚠️** 的 ID，避免在对照主表、P2 附表三、收口计划之间来回跳。

| ID | 类型 | 章节 |
|----|------|------|
| C-1 | ⚠️-MVP | **02 · P1** |
| V-1 | ⚠️-MVP | **03 · P0** |
| V-2 | ⚠️-MVP | **03 · P0** |
| V-3 | ⚠️-MVP | **03 · P0** |
| A-2 | ⚠️-MVP | **03 · P0** |
| V-4 | ⚠️-MVP | **03 · P1** |
| V-6 | ⚠️-MVP | **03 · P1** |
| D-3 | ⚠️-MVP | **03 · P1** |
| D-4 | ⚠️-MVP | **03 · P1** |
| A-5 | ⚠️-MVP | **03 · P1** |
| G-2 | ⚠️（P2 深化） | **附表三 · 01** |
| Q-5 | ⚠️（P2 深化） | **附表三 · 02** |
| K-5 | ⚠️（P2 深化） | **附表三 · 03** |
| N-5 | ⚠️（P2 深化） | **附表三 · 03** |
| H-1 | ⚠️（P2 深化） | **附表三 · 03** |

验收与 Epic：P0+P1 见 **§ P0+P1 · ⚠️ 收口计划** 与 [**P2-EPIC-BACKLOG**](./P2-EPIC-BACKLOG.md)；P2 ⚠️ 五条见 **附表三 · P2 ⚠️ 条目验收标准**、[**P2-附表三-逐条检查清单**](./P2-附表三-逐条检查清单.md)。

---

## 维护说明

- **汇总数字单一真源（SSOT）**：**本文**「总汇总」「全档位汇总」、**§ 全部 ⚠️ 一览** 与 **§ P0+P1 · ⚠️ 收口计划** 为 **108** 条计数的维护入口；规则版终局见 [**ADR 006**](../adr/006-P0P1-规则版终局指标.md)。[`00-三大核心能力升级总览.md`](./00-三大核心能力升级总览.md) 只索引、不自算小计；[`01`](./01-最强行业大脑升级方案.md) / [`02`](./02-最强直播话术升级方案.md) / [`03`](./03-最强短视频升级方案.md) **§2 仅列差距项**。  
- 若某条已在后续迭代闭合，请同步本表与 `00-三大核心能力升级总览.md` 变更记录。  
- **附表三**建议在关闭 P2 需求时做一次代码精读并更新状态；Epic 级排期见 [`P2-EPIC-BACKLOG.md`](./P2-EPIC-BACKLOG.md)；逐条 PR 勾选见 [`P2-附表三-逐条检查清单.md`](./P2-附表三-逐条检查清单.md)。  
- **自动化抽查**：[`scripts/check-upgrade-status.ps1`](../../scripts/check-upgrade-status.ps1)（Batch-B 扩展 P0/ADR 相关路径）。  
- 上一版仅 P0 文档（`P0-完成度对照表.md`）已删除；索引见 `00-三大核心能力升级总览.md`「文档索引」。
