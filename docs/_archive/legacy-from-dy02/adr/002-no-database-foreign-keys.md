# ADR-002: 不使用数据库外键约束

## 状态
已采纳

## 背景
数据库外键可以保证引用完整性，但在微服务/分库分表架构下:
1. 外键约束跨库无法生效
2. 外键导致 DDL 操作（加列、改表）需要锁表
3. 批量数据导入/删除受外键影响性能低
4. 应用层校验更灵活，可以提供更好的错误信息

## 决策
不使用数据库外键约束，改为应用层校验引用关系。

## 后果
- 正面: DDL 灵活、数据导入快、易于分库分表
- 负面: 需要应用层保证一致性，可能出现孤儿数据
- 缓解: 定期运行数据一致性检查脚本
