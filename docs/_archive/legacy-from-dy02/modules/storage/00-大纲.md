# storage 模块 — 总览大纲

> 版本：3.0 | 更新日期：2026-02-26 | 阶段：P0（系统基础支撑）

---

## 模块定位

storage 模块提供统一的文件上传和管理服务。支持多种对象存储后端（本地 / 百度 BOS / 阿里 OSS），为头像、产品图片、视频封面、AI 生成的关键帧图片、视频片段、配音音频、剪辑成片等文件上传提供标准接口。

核心职责：
1. **文件上传** — 单文件/多文件上传，自动校验类型和大小
2. **文件管理** — 文件列表、删除、URL 生成
3. **多后端支持** — 本地存储 / 百度 BOS / 阿里 OSS，通过配置切换
4. **安全校验** — 文件类型白名单、大小限制、文件名防重

> storage 模块是被其他业务模块依赖的基础服务，不依赖任何业务模块。

---

## 文档索引

| # | 文档 | 说明 | 状态 |
|---|------|------|------|
| 01 | [01-需求分析.md](01-需求分析.md) | 功能清单、业务规则（6条）、模块依赖 | ✅ 完成 |
| 02 | [02-数据库设计.md](02-数据库设计.md) | 1 张表结构、DDL、索引设计 | ✅ 完成 |
| 03 | [03-接口设计.md](03-接口设计.md) | 5 个 API 端点、上传接口详细设计、错误码 | ✅ 完成 |
| 04 | [04-存储后端系统.md](04-存储后端系统.md) | StorageProvider 接口、三种实现、切换机制、文件校验策略 | ✅ 完成 |
| 05 | [05-开发与测试.md](05-开发与测试.md) | 开发任务拆解 + 测试与验收 | ✅ 完成 |

---

## 核心架构

```
┌─────────────────────────────────────────────────────────────┐
│                  storage 模块 — 文件存储                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────────────────────────────────────┐          │
│  │            StorageController                   │          │
│  │                                               │          │
│  │  upload / upload-multi / list / delete         │          │
│  │  admin/list                                   │          │
│  └───────────────────┬───────────────────────────┘          │
│                      │                                      │
│  ┌───────────────────┴───────────────────────────┐          │
│  │            StorageService                      │          │
│  │                                               │          │
│  │  文件类型校验 → 大小校验 → UUID 命名            │          │
│  │  → StorageProvider 上传 → 保存 sys_file 记录   │          │
│  └───────────────────┬───────────────────────────┘          │
│                      │                                      │
│  ┌───────────────────┴───────────────────────────┐          │
│  │         StorageProvider 接口                    │          │
│  │                                               │          │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐       │          │
│  │  │  Local  │  │   BOS   │  │   OSS   │       │          │
│  │  │ 本地存储 │  │ 百度BOS │  │ 阿里OSS │       │          │
│  │  └─────────┘  └─────────┘  └─────────┘       │          │
│  └───────────────────────────────────────────────┘          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 核心数据

| 维度 | 数量 | 说明 |
|------|------|------|
| 数据库表 | 1 张 | sys_file |
| API 端点 | 5 个 | StorageController：upload(1), upload-multi(1), list(1), delete(1), admin/list(1) |
| 前端页面 | 1 个 | 文件管理（管理端，可选） |
| 后端任务 | 5 个 | B1-B5（SQL/Entity/Provider/Service/Controller） |
| 前端任务 | 1 个 | F1（文件管理页，可选） |
| 业务规则 | 6 条 | BR-01 到 BR-06 |
| 错误码 | 3 个 | 3701-3705 |

---

## 模块依赖关系

```
                    ┌─────────┐
                    │ common  │
                    │ config  │
                    │ vo/util │
                    │constant │
                    └────┬────┘
                         │
                    ┌────┴────┐
                    │  auth   │
                    │ 认证授权 │
                    └────┬────┘
                         │
                    ┌────┴────┐
                    │ storage │  ← 被所有需要文件上传的模块依赖
                    │ 文件存储 │
                    └────┬────┘
                         ↑
          ┌──────────────┼──────────────┐
          │              │              │
     ┌────┴────┐   ┌────┴────┐   ┌────┴────┐
     │ douyin  │   │   ai    │   │  其他    │
     │ 头像/产品│   │ 生成图片 │   │ 业务模块 │
     └─────────┘   └─────────┘   └─────────┘
```

> storage 模块依赖 auth 模块（获取当前用户信息），被所有需要文件上传的业务模块引用。
