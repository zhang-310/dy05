# 八维落地核对表（P0 + P1 + P2）

> **用途**：与 [00-前端八维升级总览.md](00-前端八维升级总览.md) 配套，逐条标注代码位置与落地状态。  
> **更新**：2026-03-21 收口批次。

---

## P0 — 直接影响 GMV（10 项）

| 编号 | 验收要点 | 主要代码位置 | 状态 |
|------|----------|--------------|------|
| D5-01 | 工作台展示 GMV、场次、在播等 | `frontend-react/src/pages/DashboardPage.tsx`，`getBusinessDashboard`（`api/dashboard.ts`） | ✅ |
| D2-01 | 场次列表展示 GMV 相关字段 | `LiveSessionPage.tsx`（DataGrid/卡片列） | ✅ |
| D2-02 | 同左，列表/卡片口径一致 | 同上 | ✅ |
| D3-01 | 脚本策划分步向导（Stepper） | `ScriptPlanningPage.tsx`（Stepper + `planStep`） | ✅ |
| D3-02 | 向导内步骤内容与移动端适配 | 同上 + `PageSkeleton`；D6-03 见 P1 | ✅ |
| D2-03 | 实时面板累计 GMV | `useLiveRealtimePanel.ts` / `useRealtimeMonitor.ts` 的 `cumulativeGmv`；`LiveRealtimePanel.tsx` | ✅ |
| D2-04 | 场次克隆 | `LiveSessionPage.tsx` + `cloneLiveSession`（`api/live.ts`）；后端 `LiveSessionServiceImpl.cloneSession` | ✅ |
| D1-01 | 全局命令面板 Ctrl+K | `GlobalCommandPalette.tsx`，`BaseLayout.tsx` 挂载 | ✅ |
| D4-01 | AI 浮动助手 | `FloatingAiAssistant.tsx`，`BaseLayout.tsx` | ✅ |
| D6-01 | PWA 离线提词（实时面板侧） | `LiveRealtimePanel` + `useNetworkStatus` / PWA 配置（见 `vite.config`） | ✅ |

---

## P1 — 提升运营效率（17 项）

| 编号 | 验收要点 | 主要代码位置 | 状态 |
|------|----------|--------------|------|
| D2-05 | 场次详情「下一步」Stepper | `LiveSessionDetailPage.tsx`（`SESSION_FLOW_STEPS` + Stepper） | ✅ |
| D2-06 | 双场次对比 + GMV/TOP 话术等 | `LiveHistoryComparePage.tsx`，`getLiveEffectivenessTopScripts` | ✅ |
| D3-03 | 短视频 Dashboard 创作步骤与项目绑定 | `ShortVideoDashboardPage.tsx`（项目焦点、阶段索引） | ✅ |
| D3-04 | Dashboard 双 Tab（今日焦点 / 数据洞察） | `ShortVideoDashboardPage.tsx`（`dashTab`） | ✅ |
| D4-02 | 话术效果 AI vs 人工归因 | `EffectivenessScorePage.tsx` + 排行 VO `aiGenerated`；后端 `ProductScriptEffectivenessService` 等（见本批次实现） | ✅ |
| D4-03 | 进化主题规则表单化 | `EvolutionTopicPage.tsx`（阈值/勾选写 `ruleConfig`） | ✅ |
| D5-03 | 统一 KPI 四维度 | `UnifiedKpiPage.tsx`，`POST /dashboard/kpi-unified` | ✅ |
| D1-02 | 最近访问与收藏 | `stores/recentVisits.ts`，`BaseLayout.tsx` 菜单 | ✅ |
| D1-03 | 运营菜单 8 核心入口 | `AdminLayout.tsx`（`OPS_QUICK_PATHS` + `creationCenterMoreChildren`） | ✅ |
| D6-02 | Drawer 响应式宽度 | `BaseLayout` / 各 Drawer 组件（`width: xs 100%` 等） | ✅ |
| D6-03 | 脚本策划移动端 sticky/换行 | `ScriptPlanningPage.tsx`（Stepper、底部 sticky） | ✅ |
| D7-01 | Getting Started 清单 | `GettingStartedChecklist.tsx`，`DashboardPage.tsx` | ✅ |
| D7-02 | What's New | `WhatsNewPopover.tsx`，`BaseLayout.tsx` | ✅ |
| D8-01 | PageSkeleton | `components/base/PageSkeleton.tsx`；Dashboard/LiveSession/ScriptPlanning 等 | ✅ |
| D8-02 | Toast 重试 + AI 请求重试 | `ToastContext.tsx`（`action`）；`request.ts`（`AI_RETRY_URL_SUBSTRINGS`） | ✅ |
| D8-03 | 错误码 suggestion | `error-codes.ts`（`errorCodeSuggestionMap` + `formatApiErrorMessage`） | ✅ |

---

## P2 — 差异化竞争力（4 项）

| 编号 | 验收要点 | 主要代码位置 | 状态 |
|------|----------|--------------|------|
| D1-04 | 全局搜索 API + 命令面板实体搜索 | `module/search`，`api/search.ts` `globalSearch`，`GlobalCommandPalette.tsx` | ✅ |
| D5-02 | 驾驶舱 CSV + cockpit-preview | `CockpitExportService` / `cockpit-preview`，`UnifiedKpiPage.tsx` | ✅ |
| D7-03 | i18n zh/en + E2E | `src/i18n/`，`e2e/a11y-i18n.spec.ts` | ✅ |
| D8-04 | eslint jsx-a11y + axe | `eslint.config.js`，`e2e/a11y-i18n.spec.ts`，`index.html` viewport | ✅ |

---

## 五主播管线（12/13）与本迭代补齐

| 项 | 说明 | 状态 |
|----|------|------|
| 拍摄任务表 + API + Admin 页 | `V098`，`ShortVideoShootingTaskController`，`ShootingTaskPage.tsx` | ✅（已有） |
| 机构/达人路由与菜单 | `router/index.tsx` + `OrgLayout` / `TalentLayout` | ✅（本批次） |
| 日更调度 `DailyScriptGenerationScheduler` | 默认关闭，见 `application.yml` | ✅（本批次） |
| 参考成片 MVP | `ShootingTaskPage` 一键提交视频任务 + 回写 `reference_*` | ✅（本批次） |

详细规格见 [13-五主播内容管线-详细规划与实施规格.md](13-五主播内容管线-详细规划与实施规格.md)。

---

## LF 系列（Live & Film 内容产能轨）

<a id="lf-系列-live--film-内容产能轨"></a>

| 编号 | 验收要点 | 主要代码位置 | 状态 |
|------|----------|--------------|------|
| LF-01 | `live_session.live_format` + Prompt 按形式选用 | Flyway **V101**；`LiveSession` / `LivePromptBuilder`；`LiveSessionFormPage` | ✅ |
| LF-02 | `script_type` 语义扩展 + 直播/短视频 `buildPrompt` 分支 | Flyway **V102**；`LivePromptBuilder`、`ShortVideoAiServiceImpl` 等；`ScriptPlanningPage` / `ScriptTab` | ✅ |
| LF-03 | 爆款二创状态机 + 列表筛选 | Flyway **V103**；`SvViralVideo`、`ViralRemakeService` / `ViralRemakeController`；`ViralLibraryPage`、`api/viral-remake.ts` | ✅ |
| LF-04 | `sv_content_calendar` + 月视图双源合并 | Flyway **V104**；`SvContentCalendarController`、`ContentCalendarServiceImpl`（`content_calendar` 条目）；`ContentCalendarPage` 日更计划对话框；`sql/shortvideo/schema.sql` §15 | ✅ |
