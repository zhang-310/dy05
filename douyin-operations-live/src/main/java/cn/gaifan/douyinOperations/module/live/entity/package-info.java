/**
 * 实体层 (Entity Layer)
 * 对应数据库表的 JPA Entity 类
 *
 * 包含的实体：
 * - LiveSession: 直播场次
 * - LiveProduct: 直播产品
 * - LiveScript: 直播话术
 * - LiveMonitor: 监控数据
 *
 * @author gaifan
 */
package cn.gaifan.douyinOperations.module.live.entity;
